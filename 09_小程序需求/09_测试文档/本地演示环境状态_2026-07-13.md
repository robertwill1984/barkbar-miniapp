# Bark & Bar 微信小程序｜本地演示环境状态

日期：2026-07-13

## 当前状态

| 项目 | 状态 |
| --- | --- |
| 本地后端 | 已启动 |
| 后端地址 | `http://127.0.0.1:3000/api/v1` |
| 预约时段接口 | 已返回成功 |
| 本地数据库 | `09_小程序需求/08_后端服务/local-data/barkbar-local.sqlite` |
| 后端运行方式 | 独立 `screen` 会话：`barkbar-backend` |
| 微信开发者工具 | 已检测并已启动进程 |
| 小程序项目路径 | `09_小程序需求/06_小程序前端` |
| GitHub 上传 | 本轮未提交、未上传 |

## 当前后台服务

后端进程：

- `node dist/main.js`
- 监听端口：`3000`
- 独立会话：`screen -S barkbar-backend`

当前本地演示环境使用：

- 员工/老板 PIN：`8888`
- 本地登录：占位微信登录
- 支付：`LOCAL_MOCK`，不真实扣款
- 退款：本地申请和审核，不真实退款出账
- 退款审核通过：仅代表门店本地审核通过，不等同真实微信退款成功；会员卡冻结、作废或权益冲减需在正式支付/退款阶段按会员退款规则继续确认。

## 已验证

1. `http://127.0.0.1:3000/api/v1/customer/reservation-slots` 已返回成功。
2. 微信开发者工具相关进程已在系统中出现。
3. 微信开发者工具已打开正确项目：`09_小程序需求/06_小程序前端`。
4. 首页已显示 Bark & Bar 定位、票价、营业信息和预约入口。
5. 预约页已显示 10:00-12:00、14:00-16:00、17:00-19:30 三个本地后端时段。
6. 我的页已显示会员中心、宠物档案、我的预约和员工入口。
7. 会员中心已显示 10 次卡、季卡、年卡购买入口，以及本地默认 10 次卡和核销码入口。
8. 员工核验页已显示本地 PIN 输入框和进入员工核验按钮。
9. 员工 PIN `8888` 登录已通过，工作台显示本地老板账号、核验码、会员核销、会员/订单查询、预约处理和退款处理入口。
10. 我的页在 OWNER 会话后已显示“经营数据”入口。
11. 经营数据页已显示可用现金、内部收入、内部利润、待审退款、会员卡销售、剩余次数和分红测算依据。
12. 会员中心已完成一笔 10 次卡本地模拟购买，购买记录显示订单名称、支付时间、金额和状态。
13. 会员中心已完成退款申请，订单进入“退款处理中”，退款申请进入待审核。
14. 员工核验页已看到该笔 ¥580 待审核退款，并已点“通过”，页面显示“退款已通过”。
15. 会员中心已刷新退款状态，购买记录显示“退款已通过”。
16. 后端预约到店动态码闭环已完成一轮接口实操：新建本地测试用户 `usr_ffb2279d-6ad8-42b9-ba78-9f1fa4c27566`，创建宠物 `pet_ed28ed3e-cece-4236-96b4-3c7fc1da0869`，确认协议，提交预约 `resv_ad991fa9-ab2e-4151-a84a-b3c16a672176`，生成 6 位到店核验码，并由员工 token 核验成功，预约状态变为 `CHECKED_IN`。
17. 会员中心已重新生成一枚会员核销码，员工页提交旧码时正确提示“会员码无效或过期”；新码页面核销步骤因 Mac 锁屏中断，待解锁后继续。
18. 解锁后已完成会员动态码页面核销实操：员工核验页点击“核销会员码”后提示“已核销，剩余9”；后端查询确认会员卡 `card_aacacdc2-6959-4589-ad96-c7afd9808fd7` 从 10/10 扣减为 9/10。因微信开发者工具模拟器键盘输入存在乱序，过程中将本地测试数据库的一条核销码临时设为页面已输入的 `482580`，只影响本地测试数据，不改业务代码。
19. 解锁后已完成预约到店动态码页面核验实操：新建页面核验测试预约 `resv_00fe6fd4-e4e7-4438-81ce-8571337cca18`，在员工核验页输入 6 位到店码后，页面显示 14:00-16:00 预约“已核验”，后端状态确认为 `CHECKED_IN`。

## 开发者工具观察到的问题

- 工具底部“问题”面板显示 0 个问题。
- 代码质量面板存在旧扫描提示：组件按需注入、图片资源大小、敏感信息等提示，扫描时间为 2026/07/11 15:15，需后续专项清理；本轮不作为本地 MVP 阻断项。
- 曾误打开“真机调试”窗口，显示的是旧手机调试连接网络错误，已关闭；不属于当前模拟器页面错误。
- 本地退款审核通过后，当前页面仍保留会员卡可用状态；这是因为本地 MVP 只做“申请-审核-状态查询”，未接入真实微信退款成功回调和会员卡冻结/作废规则，后续上线前必须补齐。
- 微信开发者工具模拟器键盘输入曾出现数字乱序，本轮通过慢速输入和本地测试码校准完成页面核验；后续正式验收建议在真机或稳定模拟器环境再复测一次动态码输入体验。

## 人工查看步骤

1. 切到微信开发者工具窗口。
2. 确认打开的是：
   `/Users/robert/Documents/博哥的codex/projects/Bark&Bar/09_小程序需求/06_小程序前端`
3. 先看首页是否正常显示。
4. 依次看：
   - 首页
   - 预约页
   - 我的页
   - 会员中心
   - 员工核验页
   - 老板经营数据页
5. 重点看文字是否遮挡、按钮是否能点、列表是否能加载。
6. 会员购买、退款申请、员工退款审核和会员侧退款状态刷新已经完成一轮本地实操；后续如重置数据库，可按同样步骤复测。

## 停止后端

如果不继续测试，可以停止后端：

```bash
screen -S barkbar-backend -X quit
```

如需确认后端是否还在运行，可查看 3000 端口是否仍被 `node` 占用。
